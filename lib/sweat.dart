import 'package:flutter/material.dart';

class SweatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome to the Level of sweating Page!'),
      ),
    );
  }
}